## 链式调用
编写方法实现下列功能
```js
// consle.log() 会默认调用参数的toString方法
console.log( add(1)(2)(3) );      //6
console.log( add(1)(2)(3)(4) );   //10

function add(x) {
  var sum = x;
  var tmp = function (y) {
    sum = sum + y;
    return tmp;
  };
  tmp.toString = function () {
    return sum;
  };
  return tmp;
}
```





```js
console.log( add(1)(2)(3) );      //6
console.log( add(1)(2)(3)(4) );   //10






```